import { StyleSheet } from 'react-native';

export const MapScreenStyling = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      
    },
    map: {
      width: '100%',
      height: '100%',
    },
  })